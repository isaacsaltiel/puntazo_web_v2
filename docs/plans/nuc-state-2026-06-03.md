# Estado consolidado de las NUCs — post-audit 2026-06-03

> Doc vivo. Consolida los reportes de los workers locales que corrieron en
> cada NUC tras el ciclo de audit disparado por
> `nuc-architecture-analysis-2026-06-03.md`. Lo actualiza el **Coordinador
> NUC** a medida que entran reportes. NO es fuente de verdad del código —
> lo es cada runner; esto es el mapa comparativo para decidir orden de trabajo.

**Reportes recibidos:** BreakPoint (audit) · WellStreet (Worker I + H) ·
Interpadel (discovery puro). Los tres entraron el 2026-06-03.

> **⚑ Decisiones del maestro aplicadas el 2026-06-03** (ver sección
> "Decisiones del maestro" abajo). Este doc ya refleja el estado
> post-rotación de PAT y post-deploy de reglas Firestore.

---

## ✅ ACCIÓN URGENTE TRANSVERSAL — RESUELTA

**Estado de credenciales tras decisiones del maestro (2026-06-03):**

| Secreto | Estado | Detalle |
|---|---|---|
| GitHub PAT `ghp_…` | ✅ **ROTADO 5/5** | Viejo revocado, nuevo con 90d expiration. WS+IP+BP rotados (204). Local dev + Actions secret `PAT_GITHUB` actualizados. BP runner PID 7152 vivo con nuevo token. |
| Password NVR `P20012001` | ⚠️ **NO se cambia (decisión consciente)** | Riesgo asumido, acotado a usuarios con LAN access al club. No volver a levantar. Saneamiento de archivos (sacar de `script.py` trackeado) sigue como deuda HP-BP. |
| Service account `puntazo-clips` JSON | 🟠 pendiente | BP raíz, no-ignorado. Se cierra con Worker HP-BP (después de J/K/L). |
| Actions secret `FIREBASE_SERVICE_ACCOUNT` | ✅ **deuda cerrada** | Estaba MISSING en GitHub Actions Secrets desde el shipping de R8 nube → `clip_edit.yml` falló 13 veces sin que nadie lo viera. Agregado y validado verde. |

---

## Tabla maestra — features Worker A-I por NUC

Leyenda: ✅ aplicado · ❌ ausente · ⚠️ variante propia · ⏳ aplicado pendiente
de activar · ➖ N/A.

| Worker | Feature | BreakPoint | WellStreet | Interpadel |
|---|---|:---:|:---:|:---:|
| A | `pulses.log` append-only + `set_state` CSV de estados | ✅ | ✅ (asumido, base modular) | ⚠️ estados PENDING/DONE/DISCARDED propios, no esquema R2; sin `pulses.log` |
| B/G | Publisher `clip_states/` Firestore | ✅ | ❌ (Worker G pendiente) | ❌ |
| C | Watchdog respeta `DEV_MODE.flag` | ✅ | — | ⚠️ watchdog Python no roba foco, pero sin `DEV_MODE.flag` |
| D/E0 | Listener `pending_pulses` Firestore (filtro club) | ✅ | ✅ (E0 hecho) | ❌ (sin Firestore) |
| D | NVR-window check pre-tx + `error_reason` | ✅ | ✅ | ❌ |
| D/E | Heartbeat `nuc_heartbeat/{clubId}` | ✅ | ✅ | ❌ (solo `heartbeat.txt` local) |
| D | Replay-on-boot FIFO por `created_at` | ✅ | ✅ | ⚠️ replay desde CSV por `source` |
| D | `_listener_close_with_error` | ✅ | ✅ | ➖ (sin listener) |
| F | match_full `_PARTIDO_<id>` | ❌ (deuda) | ✅ | ❌ |
| H | `source=upload_resumen` foto resumen | ❌ | ⏳ **aplicado, pendiente restart** | ❌ |
| I | `classify_download_error` (RTSP 404 vs TCP) | ❌ | ⏳ **aplicado, pendiente restart** | ❌ |
| E0 | Migración CSV→Firestore | ➖ | ✅ | ➖ N/A (no usa Firestore) |
| — | Multi-club mismo runner | ➖ | ✅ Pickleball+Padel | ➖ (IP solo) |

### Delta vs esperado

- **BP:** el análisis lo daba con I+H "⏳ pending". El audit confirma que
  **siguen ausentes** y que la NUC está lista para implementarlos. Sin
  sorpresas de arquitectura. **Diferencia con lo esperado:** BP NO tiene
  `config.json` — usa constantes inline en `script.py` (afecta replicabilidad
  y deja `LISTENER_NUC_ID` hardcoded).
- **WS:** el análisis daba I+H "⏳ pending"; el worker los **implementó ambos**
  en esta etapa. Quedan **aplicados en el working tree pero NO activos** (runner
  PID 25456 vivo, sin restart). Migración de cola `fs_doc_id` verificada lossless
  (311→311). Pendiente: deploy de regla Firestore (H) + línea heartbeat-watcher.js
  (I) + restart.
- **IP:** confirmado **0 de 9 workers**. Arquitectura **modular divergente**
  (`core/`+`vision/`+`runner/`, 10.7k LOC, sin `script.py`). Sin git. 5
  innovaciones mayores cosechables (ver abajo).

---

## ⚑ Decisiones del maestro (2026-06-03)

Devueltas tras revisar este doc. Aplicadas aquí.

### Infraestructura cerrada en este ciclo

- **Reglas Firestore F128-H2 + F130 → DEPLOYADAS a Console** (ruleset
  `da0d0727`). `pending_pulses` ahora acepta `create` con
  `source=upload_resumen` + `delete` por `uid_creator`. **Worker H queda
  desbloqueado end-to-end.** El camino del deploy (REST API + service
  account) quedó documentado en
  [firebase-admin-capabilities.md](firebase-admin-capabilities.md) como
  memoria para no reinventarlo.
- **Worker I, 1-línea en `heartbeat-watcher.js` → YA CUBIERTO.**
  `assets/heartbeat-watcher.js:83` usa OR para ambos valores
  (`nvr_window_exceeded` || `nvr_window_exceeded_late`). Cero cambio.
- **PAT GitHub rotado 5/5** y **`FIREBASE_SERVICE_ACCOUNT` agregado a
  Actions** (ver tabla de credenciales arriba).

### Resolución de las contradicciones que escalé

1. **Password NVR `P20012001`** → **NO se cambia.** Decisión consciente:
   riesgo acotado a usuarios con LAN access al club. No volver a levantar.
2. **WS `allowed_clubs` solo Pickleball** → **INTENCIONAL, no bug.** El
   worker WS lo blindó con `tools/validate_match_full.py:104`
   (`match_full_not_allowed` para Padel). El mecanismo Worker F **es**
   multi-club; solo el rollout está acotado. Padel se habilita cuando el
   operador lo agregue a `allowed_clubs` (1 línea de config).
3. **IP `BUTTON_QUEUE_CSV` → `BP_Puntazo`** → **copy-paste con hueco real**
   en el filtro de 3ª capa (`button_csv.py:357-359`). Resolución en dos
   pasos:
   - **(1) AHORA:** el worker IP setea `forms_csv` y `button_csv` a `""`
     en el config de IP. Cero riesgo, cierra el hueco inmediato (IP no
     tiene flujo Forms operativo — los jobs viejos fallan con
     `match_full_not_allowed`).
   - **(3) DESPUÉS:** **Worker M** = migración Forms→POST `/trigger` en IP
     (Cloudflare Tunnel + `NUC_BASE_URL` + `PUNTAZO_DASH_TOKEN`). Elimina
     el polling de Drive de raíz.

### Cosechables IP — decisiones finales

| # | Innovación | Decisión |
|---|---|---|
| 1 | Pipeline modular `core/`+`vision/` | **Mantener exclusiva en IP.** |
| 2 | Visión-pose YOLOv8m | **Esperar.** Portar solo si IP valida <5% falsos positivos en 1 mes de operación. |
| 3 | Dashboard Flask | **Worker K** (después de J). |
| 4 | Watchdog Python + disk-monitor + Telegram | **Worker J.** |
| 5 | NVR discovery auto-recuperación IP | **Worker N aparte.** Lectura del coordinador: el audit reporta `core/nvr_discovery.py` = **390 LOC**, excede el umbral de 200 que fijó el maestro para plegarlo en J → va separado. |

### Workers nuevos aprobados (drafts a redactar por el coordinador)

| Worker | Qué | Cosecha |
|---|---|---|
| **J** | Watchdog Python + disk-monitor + Telegram → portar IP a BP/WS | innovación #4 |
| **K** | Dashboard Flask → portar IP a BP/WS (después de J) | innovación #3 |
| **L** | Sync de assets compartidos (logo animado + outro + fuentes) entre las 3 NUCs. Approach: Dropbox `/Puntazo/Assets/` + `rclone copy` al boot + cron 1h. Cero infra nueva. Logo unificado = el animado de BP (`puntazo_anim.webm`). WS+IP pasan de PNG estático a animado. | logo animado |
| **M** | Forms→POST `/trigger` en IP (ver decisión 3) | — |
| **N** | NVR discovery auto-recuperación IP → portar a BP/WS | innovación #5 |
| **HP-IP** | Saneamiento secretos IP + `.gitignore` del SA JSON | — |
| **HP-BP** | Saneamiento secretos BP (sacar pwd NVR de `script.py` trackeado + ignorar SA JSON). **Después** de J/K/L. | — |

> **Gate:** ningún brief nuevo se manda a las NUCs hasta que el maestro
> apruebe cada draft. Los drafts van en branch `coordinador-briefs-2026-06-XX`,
> aviso antes de pushear.

### Worker L — estado 2026-06-05

- **BreakPoint:** ✅ seed + sync instalado + restart controlado completado.
  `asset_sync` quedó activo como thread daemon, con estado en
  `media/Prod/.assets_state.json`; `global__outro` aplicó el nuevo
  `outro.mp4` central y los demás assets quedaron en noop por hash correcto.
  Commit local BP `eae67e6` agrega Worker L + QSV opcional; QSV queda apagado
  por defecto (`USE_QSV_ENCODER=False`) hasta validacion con pulso real.
- **Central:** ✅ `nuc_assets/global__outro` publicado desde la PC central en
  `/Puntazo/assets/global/v1__outro.mp4`.
- **WellStreet:** ✅ seed de `logo_club` completado para
  `WellStreet-Pickleball` y `WellStreet-Padel`; ambos usan el mismo
  `wellstreet.png` actual, remoto local `nombre:`. Worker L instalado y sync
  manual aplicado; `global__outro` ya reemplazo `outro.mp4`. Falta restart
  controlado para activar thread automatico.
- **Memoria completa:** ver
  [nuc-assets-worker-l-2026-06-05.md](nuc-assets-worker-l-2026-06-05.md).
- **Pendiente:** restart controlado en WellStreet y discovery/seed en Interpadel.

### Firestore para Interpadel = SÍ

Decisión del maestro. Secuencia IP definitiva:

1. Rotar PAT (✅ hecho).
2. **HP-IP** (saneamiento secretos + `.gitignore` del SA JSON).
3. **A** (`pulses.log`).
4. **B + D + E0** (Firestore listener).
5. **G** (clip_states).
6. **I**.
7. **H**.
8. **F** (solo si el cliente IP pide match_full).

---

## Constantes clave por NUC

| Constante | BreakPoint | WellStreet | Interpadel |
|---|---|---|---|
| `LISTENER_CLUB` | `BreakPoint` | `WellStreet-Pickleball` (+Padel) | — (sin listener) |
| `LISTENER_NUC_ID` | `BreakPoint-NUC` (hardcoded) | `WellStreet-NUC` | ➖ |
| NVR retention (ventana) | 7d | 7d | 14d (config) |
| Retention local media | 5d | — | `maintenance.retention_days` |
| Heartbeat interval | 30s | 30s | `heartbeat.txt` local |
| NVR modelo | DS-7608NI-Q1/8P | (192.168.33.4) | DS-7604NI-Q1/4P @ 192.168.100.2 |
| NVR firmware | V4.83.100 b250626 | (pendiente confirmar) | V4.83.100 b250626 |
| Canchas → canal | 1-8 → 101..801 | 1-6, swap 5↔6 (601/501) | 3→201, 4→301, 5→101, 6→401 |
| Logo izquierdo | **animado** `puntazo_anim.webm` (VP9 alpha) | (pendiente confirmar) | estático `puntazo.png` |
| Logo derecho | `BreakPoint.png` estático | — | `interpadel.png` estático |
| Dropbox dest | `dropbox:/Puntazo/Locaciones` | rclone | `dropbox:/Puntazo/Locaciones/{club}/{court}/{lado}` |

⚠️ **Inconsistencia BP:** `NVR_RETENTION_DAYS=7` pero `nvr_retention_days`
no aplica igual en WS — WS reporta desajuste retention_days=5 (limpieza local)
vs nvr_window=14. Revisar coherencia ventana-vs-limpieza por NUC (el
`nvr_window_exceeded_late` de I asume la ventana NVR, no la limpieza local).

⚠️ **Logo animado: solo BP lo tiene.** BP integró el 2026-06-03 11:32
`puntazo_anim.webm` (VP9 alpha) desde `puntazo_logo_animation.gif`. WS e IP
siguen con PNG estático. → **Worker L (sync de assets vía Dropbox)** aprobado:
unifica las 3 NUCs al logo animado de BP. Ver decisiones del maestro arriba.

---

## Interpadel — innovaciones cosechables

El audit de IP reportó 5 innovaciones mayores + 5 menores. **Decisiones del
maestro aplicadas** (columna final):

| # | Innovación | Archivo (IP) | Decisión final |
|---|---|---|---|
| 1 | Pipeline modular `core/`+`vision/` | ~10 módulos | **Mantener exclusiva en IP.** Reescribir BP/WS a modular es refactor fuera de ciclo. Deuda estratégica anotada. |
| 2 | Visión-pose como fuente de eventos (YOLOv8m-pose) | `vision/vision_multi.py` +856 | **Esperar datos.** Portar solo si IP valida <5% falsos positivos en 1 mes de operación. |
| 3 | Dashboard Flask (MJPEG + STOP + POST /trigger) | `core/dashboard.py` +872 | **→ Worker K** (después de J). |
| 4 | Watchdog Python + disk-monitor + Telegram | `runner/watchdog.py` +397, `core/maintenance.py` | **→ Worker J.** Reemplaza el watchdog ad-hoc PowerShell de BP/WS. |
| 5 | NVR discovery con auto-recuperación de IP | `core/nvr_discovery.py` +390 | **→ Worker N aparte** (390 LOC > umbral 200 para plegarlo en J). |

Menores observables (no worker-grade, anotados): dispatch inmediato post-persist,
cleanup automático de `.mp4`, notifier anti-spam centralizado, hardening Windows
(`windows_hardening_admin.ps1`), STOP.flag en caliente vía `stop_event` global.

**Anti-patrón a NO replicar:** secretos en `config.json` en claro (mismo
problema que WS/BP, no es innovación).

---

## Pendientes percibidos por NUC (de los reportes)

### BreakPoint
- 🔴 Secretos en repo (pwd NVR en `script.py` trackeado + SA JSON en raíz no-ignorado).
- `config.json` inexistente → constantes inline → `LISTENER_NUC_ID` hardcoded.
- `ffmpeg_force_web_compat` quedó código muerto tras fusión 3→2 encodes.
- `DEV_MODE.flag` presente AHORA — si BP es prod, borrarlo para que el watchdog recupere foco.

### WellStreet
- ⏳ **Restart pendiente** para activar I+H (decisión de Isaac, regla de no tocar prod en operación).
- 🌐 **Coordinación web requerida** (ver abajo) — bloquea la activación completa de I+H.
- 🔴 Secretos en `config.json` (HP no ejecutado todavía).
- `match_recording.allowed_clubs` = solo Pickleball → **intencional** (resuelto, ver decisiones).
- Validaciones que tocan Firestore prod quedaron ⏭️ → desbloqueadas tras deploy de reglas `da0d0727`.

### Interpadel
- `NUC_BASE_URL` Apps Script = placeholder (migración POST /trigger bloqueada: sin Cloudflare Tunnel, sin port-forward).
- `PUNTAZO_DASH_TOKEN` no seteado → dashboard sin auth en LAN.
- `telegram_bot_token` = placeholder → todas las alertas se pierden (lo cierra Worker J).
- `BUTTON_QUEUE_CSV` apunta a `BP_Puntazo` → **copy-paste con hueco real** (resuelto: fix inmediato `""` + Worker M).
- Watchdog probablemente **no corriendo ahora** (`BASE/logs/watchdog.log` no existe).
- Forms CSV con jobs viejos fallando con FFmpeg exit 0xCFB200B8 (RTSP 404) → sin Worker I no se distingue de timeout → 5 retries.

---

## Coordinación web requerida — ✅ RESUELTA

1. **Worker I → línea en `assets/heartbeat-watcher.js`** → ✅ **ya cubierto**.
   `heartbeat-watcher.js:83` usa OR para ambos valores. Cero cambio.
2. **Worker H → regla Firestore `pending_pulses` create `source=upload_resumen`**
   → ✅ **DEPLOYADA** (ruleset `da0d0727`, incluye también `delete` por
   `uid_creator`). Worker H end-to-end desbloqueado. El cliente debe hacer poll
   de `pending_pulses/{id}` hasta `consumed_at != null` (lado web, iteración aparte).
3. **WS:** queda solo el **restart** del runner para activar I+H. Sugerencia del
   worker: bumpear `firestore.version` a `E-1.2.0` en el restart para ver H+I
   vivos en el heartbeat. Decisión de timing de Isaac (no tocar prod en operación).

---

## Orden de trabajo recomendado (propuesta — Isaac decide)

### BreakPoint
1. Confirmar audit (ya está) → implementar **Worker I** y **Worker H** (ausentes,
   plan ya previsto en el análisis). Branches separadas por worker.
2. **Worker HP-BP** — sacar pwd NVR de `script.py` trackeado + ignorar SA JSON.
   PAT ya rotado; pwd NVR no se cambia (decisión) pero igual sale del archivo
   trackeado. **Después de J/K/L.**

### WellStreet
1. **Restart** para activar I+H (tras deploy de coordinación web #1+#2).
2. Ejecutar **HP** (sanear secretos) — sigue pendiente.
3. Naturales siguientes: **G** (clip_states — la web no ve progreso por-clip en WS hoy).

### Interpadel — Firestore = SÍ (decisión del maestro)
Secuencia definitiva:
1. Rotar PAT (✅ hecho).
2. **HP-IP** (saneamiento secretos + `.gitignore` SA JSON). + el worker IP
   setea `forms_csv`/`button_csv` a `""` para cerrar el hueco del cruce de colas.
3. **A** (`pulses.log`).
4. **B + D + E0** (Firestore listener).
5. **G** (clip_states).
6. **I**.
7. **H**.
8. **F** (solo si el cliente IP pide match_full).

Cosecha primero (J/K/L/N a BP/WS) corre en paralelo al onboarding de IP — son
tracks independientes.

---

## Contradicciones — ✅ RESUELTAS por el maestro

- **IP `BUTTON_QUEUE_CSV` = `BP_Puntazo`** → copy-paste con hueco real
  (`button_csv.py:357-359`). Fix (1) ahora: `forms_csv`/`button_csv` = `""`.
  Fix (3) definitivo: **Worker M**.
- **WS `allowed_clubs` solo Pickleball** → **intencional**, blindado por
  `tools/validate_match_full.py:104`. Padel se habilita con 1 línea de config
  cuando el operador lo decida.
- **Logo BP animado vs WS/IP estático** → unificar a animado vía **Worker L**.

## Deudas operativas abiertas

- **BP:** runner PID 7152 OK con nuevo token, pero secretos en repo
  (`script.py:85` trackeado + SA JSON no-ignorado) → **Worker HP-BP** (después
  de J/K/L).
- **`nvr.password` en `config.json` en claro** → migrar a `.env` en próxima
  pasada (no urgente, riesgo asumido).
- **WS:** restart pendiente para activar I+H.

---

*Consolidado por el Coordinador NUC el 2026-06-03. Actualizado el 2026-06-04
con las decisiones del maestro (rotación PAT, deploy reglas, workers J/K/L/M/N
aprobados, Firestore-IP=SÍ).*
