{
  "name": "puntazo-functions",
  "description": "Cloud Functions de Puntazo — ranking autoritativo, confirmacion, notificaciones",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "vendor": "node scripts/vendor-ranking.js",
    "pretest": "npm run vendor",
    "test": "node --test",
    "serve": "npm run vendor && firebase emulators:start --only functions,firestore,auth"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1"
  }
}
